<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net10.0-windows</TargetFramework>
        <OutputType>WinExe</OutputType>
        <UseWindowsForms>true</UseWindowsForms>
        <Nullable>enable</Nullable>
        <ImplicitUsings>enable</ImplicitUsings>
    </PropertyGroup>

    <!-- 🔹 Ссылки на другие проекты (раскомментируй, если нужны сервисы) -->
    <!--
    <ItemGroup>
        <ProjectReference Include="..\ApartmentRentalSystem.Core\ApartmentRentalSystem.Core.csproj" />
        <ProjectReference Include="..\ApartmentRentalSystem.Application\ApartmentRentalSystem.Application.csproj" />
    </ItemGroup>
    -->

    <!-- 🔹 Пакеты (можно убрать для чистой оболочки) -->
    <!--
    <ItemGroup>
        <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" Version="10.0.0" />
    </ItemGroup>
    -->

</Project>